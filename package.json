{
  "name": "gcp-express-firestore-sandbox",
  "version": "1.0.0",
  "description": "template for GCP Firestore and vanilla Node API routes",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Taylor Pasquariello",
  "license": "ISC",
  "dependencies": {
    "colors": "^1.4.0",
    "dotenv": "^16.0.0",
    "express": "^4.17.2"
  },
  "devDependencies": {
    "babel-eslint": "^10.1.0"
  }
}
